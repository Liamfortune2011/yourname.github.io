(function(){
if(window.__GAMEHUB_COOKIE_OVERHAUL__)return;window.__GAMEHUB_COOKIE_OVERHAUL__=true;
function start(){var v=document.getElementById('view-cookie');if(!v||v.__cookieNew)return;v.__cookieNew=true;var old=v.querySelector('.cookie-wrap');if(old)old.style.display='none';
var box=document.createElement('div');box.className='cookie-new-box';box.innerHTML='<div class="cookie-main"><h2>🍪 Cookie Clicker</h2><div class="cookie-count" id="cc-count">0 cookies</div><div class="cookie-cps" id="cc-cps">0 cookies/sec</div><button class="cc-big" id="cc-click">🍪</button><div class="cc-power" id="cc-power">+1 per click</div><div class="cc-actions"><button id="cc-export">Export Save</button><button id="cc-import">Import Save</button><button id="cc-reset">Reset</button></div><div class="cc-code" id="cc-code"></div></div><div class="cookie-store"><h3>Store</h3><div id="cc-store"></div></div>';
v.appendChild(box);
var buildings=[['Cursor',15,0.1],['Grandma',100,1],['Farm',500,8],['Mine',2500,40],['Factory',12000,200],['Bank',60000,1000]], upgrades=[['Better Clicks',100,1],['Golden Clicks',2500,4],['Mega Clicks',50000,15]];
var st={cookies:0,total:0,clickPower:1,best:0,buildings:[0,0,0,0,0,0],upgrades:[false,false,false]};
function load(){try{var x=JSON.parse(localStorage.getItem('gamehub_cookie_save')||'null');if(x&&Array.isArray(x.buildings)&&Array.isArray(x.upgrades)){st=Object.assign(st,x);st.buildings=st.buildings.slice(0,6);while(st.buildings.length<6)st.buildings.push(0);st.upgrades=st.upgrades.slice(0,3);while(st.upgrades.length<3)st.upgrades.push(false)}}catch(e){}}
function save(){localStorage.setItem('gamehub_cookie_save',JSON.stringify(st));if(window.saveGameHubScore)window.saveGameHubScore('cookie',Math.floor(st.best||st.cookies))}
function cps(){return buildings.reduce(function(n,b,i){return n+st.buildings[i]*b[2]},0)}
function fmt(n){return Math.floor(n).toLocaleString()}
function render(){document.getElementById('cc-count').textContent=fmt(st.cookies)+' cookies';document.getElementById('cc-cps').textContent=cps().toFixed(1)+' cookies/sec';document.getElementById('cc-power').textContent='+'+fmt(st.clickPower)+' per click';var s=document.getElementById('cc-store');s.innerHTML='';buildings.forEach(function(b,i){var cost=Math.floor(b[1]*Math.pow(1.15,st.buildings[i]));var d=document.createElement('div');d.className='cc-item';d.innerHTML='<div><b>'+b[0]+'</b><small> '+b[2]+' CPS · Owned: '+st.buildings[i]+'</small></div><button>Buy '+fmt(cost)+'</button>';d.querySelector('button').onclick=function(){if(st.cookies>=cost){st.cookies-=cost;st.buildings[i]++;render();save()}};s.appendChild(d)});upgrades.forEach(function(u,i){if(st.upgrades[i])return;var d=document.createElement('div');d.className='cc-item';d.innerHTML='<div><b>'+u[0]+'</b><small> +'+u[2]+' click power</small></div><button>Buy '+fmt(u[1])+'</button>';d.querySelector('button').onclick=function(){if(st.cookies>=u[1]){st.cookies-=u[1];st.clickPower+=u[2];st.upgrades[i]=true;render();save()}};s.appendChild(d)})}
function code(){var raw=JSON.stringify({v:1,cookies:st.cookies,total:st.total,clickPower:st.clickPower,best:st.best,buildings:st.buildings,upgrades:st.upgrades});return'GHCOOKIE1.'+btoa(unescape(encodeURIComponent(raw)))}
function decode(x){try{if(x.indexOf('GHCOOKIE1.')!==0)throw 0;var raw=decodeURIComponent(escape(atob(x.slice(11))));var d=JSON.parse(raw);if(!Array.isArray(d.buildings)||!Array.isArray(d.upgrades))throw 0;st={cookies:Math.max(0,Number(d.cookies)||0),total:Math.max(0,Number(d.total)||0),clickPower:Math.max(1,Number(d.clickPower)||1),best:Math.max(0,Number(d.best)||0),buildings:d.buildings.slice(0,6).map(function(n){return Math.max(0,Math.floor(Number(n)||0))}),upgrades:d.upgrades.slice(0,3).map(Boolean)};while(st.buildings.length<6)st.buildings.push(0);while(st.upgrades.length<3)st.upgrades.push(false);return true}catch(e){return false}}
document.getElementById('cc-click').onclick=function(){st.cookies+=st.clickPower;st.total+=st.clickPower;st.best=Math.max(st.best,st.cookies);render()};document.getElementById('cc-export').onclick=function(){var c=code();var el=document.getElementById('cc-code');el.textContent=c;try{navigator.clipboard.writeText(c);el.textContent='Save code copied! '+c}catch(e){el.textContent=c}};document.getElementById('cc-import').onclick=function(){var c=prompt('Paste your Cookie Clicker save code:');if(c&&decode(c.trim())){save();render();document.getElementById('cc-code').textContent='Save imported successfully!'}else if(c)alert('Invalid Cookie Clicker save code.')};document.getElementById('cc-reset').onclick=function(){if(confirm('Reset your Cookie Clicker progress?')){st={cookies:0,total:0,clickPower:1,best:0,buildings:[0,0,0,0,0,0],upgrades:[false,false,false]};save();render()}};load();render();setInterval(function(){if(!document.getElementById('view-cookie').classList.contains('active'))return;var n=cps();if(n){st.cookies+=n/2;st.total+=n/2;st.best=Math.max(st.best,st.cookies);render()}},500);setInterval(save,5000)}
window.startCookieOverhaul=start;if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',start);else start();
})();
