window.extraGames=window.extraGames||{};window.extraGames.subaway=function(){
var c=by('sr-canvas'),ctx=c.getContext('2d'),W=c.width,H=c.height,raf,score=0,best=0,speed=260,lane=1,targetLane=1,playerY=0,vy=0,over=false,credits=0,items=[],obstacles=[],spawn=0;
function reset(){score=0;credits=0;lane=1;targetLane=1;playerY=H-80;vy=0;over=false;speed=260;items=[];obstacles=[];by('sr-score').textContent=0;by('sr-msg').textContent='A/D or ←/→ to switch lanes · Space/W to jump.';if(raf)cancelAnimationFrame(raf);raf=requestAnimationFrame(loop)}
function act(a){if(over)return;if(a==='left'||a==='ArrowLeft'||a==='a')targetLane=Math.max(0,targetLane-1);if(a==='right'||a==='ArrowRight'||a==='d')targetLane=Math.min(2,targetLane+1);if(a==='jump'||a===' '||a==='w'){if(playerY>=H-82){vy=-620}}}
function loop(now){var last=loop.last||now;var dt=Math.min(.033,(now-last)/1000);loop.last=now;if(over){draw();return}lane+=(targetLane-lane)*Math.min(1,dt*12);vy+=1500*dt;playerY+=vy*dt;if(playerY>H-82){playerY=H-82;vy=0}speed+=dt*5;score+=dt*10;spawn-=dt;if(spawn<=0){spawn=.7+Math.random()*.7;var l=Math.floor(Math.random()*3);obstacles.push({lane:l,y:-30,w:42,h:30});if(Math.random()<.65)items.push({lane:Math.floor(Math.random()*3),y:-20,r:9})}obstacles.forEach(function(o){o.y+=speed*dt});items.forEach(function(i){i.y+=speed*dt});obstacles=obstacles.filter(function(o){return o.y<H+40});items=items.filter(function(i){if(i.y>H+30)return false;var px=lane*W/3+W/6;if(Math.abs(px-(i.lane*W/3+W/6))<28&&Math.abs(playerY-i.y)<30){credits++;score+=50;return false}return true});obstacles.forEach(function(o){if(Math.abs(lane-o.lane)<.35&&o.y+o.h>playerY+5&&o.y<playerY+30&&Math.abs(vy)<100){over=true;best=Math.max(best,Math.floor(score));by('sr-best').textContent=best;by('sr-msg').textContent='Run over! Score '+Math.floor(score)+'. Press New Run.'}});by('sr-score').textContent=Math.floor(score);by('sr-credits').textContent=credits;draw();raf=requestAnimationFrame(loop)}
function draw(){ctx.fillStyle='#6b88a8';ctx.fillRect(0,0,W,H);ctx.fillStyle='#333';ctx.fillRect(0,H-50,W,50);for(var i=1;i<3;i++){ctx.strokeStyle='#777';ctx.lineWidth=3;ctx.beginPath();ctx.moveTo(i*W/3,0);ctx.lineTo(i*W/3,H);ctx.stroke()}items.forEach(function(i){ctx.fillStyle='#f2c94c';ctx.beginPath();ctx.arc(i.lane*W/3+W/6,i.y,i.r,0,7);ctx.fill()});obstacles.forEach(function(o){ctx.fillStyle='#d9534f';ctx.fillRect(o.lane*W/3+W/6-o.w/2,o.y,o.w,o.h)});ctx.fillStyle='#4f9cf0';ctx.fillRect(lane*W/3+W/6-14,playerY,28,32);if(over){ctx.fillStyle='rgba(0,0,0,.5)';ctx.fillRect(0,0,W,H);ctx.fillStyle='#fff';ctx.font='bold 26px sans-serif';ctx.textAlign='center';ctx.fillText('Game Over',W/2,H/2)}}
window.addEventListener('keydown',function(e){if(!by('view-subaway').classList.contains('active'))return;if(e.key==='ArrowLeft'||e.key.toLowerCase()==='a'){e.preventDefault();act('left')}if(e.key==='ArrowRight'||e.key.toLowerCase()==='d'){e.preventDefault();act('right')}if(e.key===' '||e.key.toLowerCase()==='w'||e.key==='ArrowUp'){e.preventDefault();act('jump')}});c.addEventListener('click',function(e){var r=c.getBoundingClientRect(),mx=e.clientX-r.left;act(mx<W/2?'left':'right')});by('sr-new').onclick=function(){confirmReset(reset)};reset();};
