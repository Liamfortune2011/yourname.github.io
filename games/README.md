# Game modules

The refactor keeps the live site unchanged until the extracted JavaScript modules are validated. The splitter rebuilds this directory directly from `main/index.html` so the game code stays identical.