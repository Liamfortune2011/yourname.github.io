# Game modules

This folder is reserved for individual game modules. The current site keeps the original game code in `index.html` while the refactor is staged here so the live site is not broken during the move.

Each game will eventually have its own JavaScript file, with shared UI/auth/leaderboard code kept outside this folder.
