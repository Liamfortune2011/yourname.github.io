/* tag */
  #tag-stats { display:flex; gap:16px; font-size:14px; color:var(--muted); margin-bottom:0.75rem; flex-wrap:wrap; justify-content:center; }
  #tag-canvas {
    background: #e8e6dc;
    border: 2px solid var(--app-text);
    border-radius: 10px;
    margin-bottom: 1rem;
    touch-action: none;
    max-width: 100%;
    width: min(96vw, 720px);
    height: auto;
  }
  #tag-msg { font-size: 13px; color: var(--muted); margin-bottom: 0.5rem; min-height: 20px; text-align:center; }
  .tag-opts { display:flex; gap:8px; margin-bottom: 0.75rem; flex-wrap:wrap; justify-content:center; }
  .power-legend { font-size:12px; color:var(--muted); margin-bottom:0.75rem; text-align:center; }
  .power-legend span { margin: 0 6px; }
